import { useState } from 'react';
import { Image, KeyboardAvoidingView, Platform, ScrollView, StyleSheet, Text, View } from 'react-native';
import { Link, router } from 'expo-router';
import { Button, Field, Label } from '../../components/UI';
import { configured, supabase } from '../../lib/supabase';
import { colors } from '../../theme';
export default function Login() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit() { setError(''); if (!configured) return setError('Add your Supabase values to .env before signing in.'); setBusy(true); const { error: authError } = await supabase.auth.signInWithPassword({ email: email.trim(), password }); setBusy(false); if (authError) setError(authError.message); else router.replace('/'); } async function reset() { if (!email.includes('@')) return setError('Enter your email first.'); const { error: resetError } = await supabase.auth.resetPasswordForEmail(email.trim(), { redirectTo: 'aipms://reset-password' }); setError(resetError?.message || 'Password reset instructions sent.'); } return <KeyboardAvoidingView style={styles.page} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={styles.brand}><Image source={require('../../../assets/app_logo.png')} style={styles.mark} resizeMode="contain" /><View><Text style={styles.brandName}>AIPMS</Text><Text style={styles.brandLong}>Anatsor Integrated Poultry Management System</Text></View></View><View style={styles.hero}><Label>Secure farm monitoring</Label><Text style={styles.title}>Welcome back</Text><Text style={styles.copy}>Sign in to see live readings, farm records and alerts.</Text></View><View style={styles.form}><Field label="Email address" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" /><Field label="Password" value={password} onChangeText={setPassword} secureTextEntry />{error ? <Text style={styles.error}>{error}</Text> : null}<Button title="Sign in" loading={busy} onPress={submit} /><Text onPress={reset} style={styles.link}>Forgot password?</Text></View><Text style={styles.foot}>New here? <Link href="/(auth)/signup" style={styles.link}>Create an account</Link></Text></ScrollView></KeyboardAvoidingView>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.paper }, content: { flexGrow: 1, padding: 24, paddingTop: 60, justifyContent: 'center', maxWidth: 520, width: '100%', alignSelf: 'center' }, brand: { flexDirection: 'row', alignItems: 'center', gap: 12, marginBottom: 52 }, mark: { width: 48, height: 54 }, brandName: { color: colors.barn, fontWeight: '900', fontSize: 21 }, brandLong: { color: colors.barnSoft, fontSize: 10, maxWidth: 240 }, hero: { gap: 10, marginBottom: 30 }, title: { color: colors.barn, fontSize: 38, lineHeight: 42, fontWeight: '900' }, copy: { color: colors.barnSoft, fontSize: 16, lineHeight: 23 }, form: { gap: 18 }, error: { color: colors.bad, backgroundColor: '#FCEDE8', padding: 12, borderRadius: 12 }, link: { color: colors.amberDeep, fontWeight: '800', textAlign: 'center' }, foot: { textAlign: 'center', color: colors.barnSoft, marginTop: 28 } });
