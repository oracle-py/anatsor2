export type Device = { id: string; name: string; owner_user_id: string | null; local_hostname: string | null; status: 'online' | 'offline' | 'unknown'; last_seen_at: string | null };
export type Reading = { id: number; device_id: string; temperature: number | null; humidity: number | null; air_quality: number | null; mq_baseline: number | null; fan_on: boolean; humidifier_on: boolean; alarm_active: boolean; temperature_high: boolean; temperature_low: boolean; humidity_high: boolean; humidity_low: boolean; air_quality_bad: boolean; camera_live: boolean | null; detection_label: 'rat' | 'snake' | null; detection_confidence: number | null; detection_at: string | null; created_at: string };
export type DailyRecord = { id: string; device_id: string; owner_user_id: string; record_date: string; chicks_hatched: number; birds_bred: number; eggs_collected: number; mortality_count: number; note: string | null; created_at: string; updated_at: string };
export type Alert = { id: string; device_id: string; type: 'temp_high' | 'temp_low' | 'humidity_high' | 'humidity_low' | 'air_bad' | 'device_offline'; severity: 'warning' | 'critical'; started_at: string; resolved_at: string | null; read_at: string | null; message: string };
